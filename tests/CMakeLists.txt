cmake_minimum_required(VERSION 3.15)

# 简单的测试框架（后续可以集成 Google Test）
enable_testing()

# 示例测试（后续会添加更多）
# add_executable(test_server test_server.cpp)
# target_link_libraries(test_server ChatGPTServer)
# add_test(NAME ServerTest COMMAND test_server)

